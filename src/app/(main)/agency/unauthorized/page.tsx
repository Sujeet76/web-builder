import UnAuthorizedComponent from "@/components/unauthorized";

interface Props {}

const UnAuthorizedPage: React.FC<Props> = (props) => {
  return <UnAuthorizedComponent />;
};

export default UnAuthorizedPage;
